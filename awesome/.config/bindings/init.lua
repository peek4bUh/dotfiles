return {
    client = {
        key = require 'bindings.client.keyboard',
        mouse = require 'bindings.client.mouse',
    },
    global = {
        key = require 'bindings.global.keyboard',
        mouse = require 'bindings.global.mouse',
    },
    mod = require 'bindings.modifiers'
}
